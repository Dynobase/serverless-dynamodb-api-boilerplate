{
    "definitions": {},
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "title": "Thing",
    "required": ["name"],
    "properties": {
      "name": {
        "type": "string",
        "title": "Name of the thing",
        "default": "",
        "pattern": "^[a-zA-Z0-9]+$"
      }, "status": {
        "type": "string",
        "title": "Status of the thing",
        "default": "OK",
        "pattern": "^[a-zA-Z0-9]+$"
      }
    }
  }